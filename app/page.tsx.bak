import { motion } from "framer-motion";
import { useMemo } from "react";

export default function Home() {
  const sakura = useMemo(() => Array.from({ length: 18 }), []);

  return (
    <div className="relative min-h-screen overflow-hidden">
      {/* Optimized Background */}
      <div className="absolute inset-0 -z-10 bg-[url('/bg-japan.jpg')] bg-cover bg-center" />

      {/* Sakura */}
      {sakura.map((_, i) => (
        <motion.span
          key={i}
          className="absolute text-pink-300 text-xl select-none"
          style={{ left: `${Math.random() * 100}%` }}
          animate={{ y: ["-10%", "110%"], rotate: [0, 360] }}
          transition={{ duration: 12 + Math.random() * 8, repeat: Infinity }}
        >
          ❀
        </motion.span>
      ))}

      {/* Content */}
      <main className="flex items-center justify-center min-h-screen px-6">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8 }}
          className="bg-black/60 backdrop-blur-xl p-10 rounded-3xl border border-white/20 max-w-xl text-center"
        >
          <h1 className="text-4xl md:text-5xl font-bold mb-4">
            和風ポートフォリオ
          </h1>
          <p className="text-white/80 mb-6">
            Japanese style website – production optimized.
          </p>
          <div className="flex justify-center gap-4">
            <button className="px-6 py-2 rounded-2xl bg-white text-black hover:scale-105 transition">
              Masuk
            </button>
            <button className="px-6 py-2 rounded-2xl border border-white hover:bg-white hover:text-black transition">
              Tentang
            </button>
          </div>
        </motion.div>
      </main>
    </div>
  );
}
      <div className="absolute inset-0 -z-10">
        <div className="absolute inset-0 bg-[url('https://images.unsplash.com/photo-1549692520-acc6669e2f0c')] bg-cover bg-center opacity-40" />
        <motion.div
          className="absolute inset-0 bg-gradient-to-t from-black via-transparent to-black"
          animate={{ opacity: [0.6, 0.8, 0.6] }}
          transition={{ duration: 6, repeat: Infinity }}
        />
      </div>

      {/* Sakura */}
      {[...Array(25)].map((_, i) => (
        <motion.span
          key={i}
          className="absolute text-pink-300 text-xl"
          style={{ left: `${Math.random() * 100}%` }}
          animate={{ y: ["-10%", "110%"], rotate: [0, 360] }}
          transition={{ duration: 10 + Math.random() * 10, repeat: Infinity }}
        >
          ❀
        </motion.span>
      ))}

      {/* Content */}
      <main className="flex items-center justify-center min-h-screen px-6">
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          className="bg-black/60 backdrop-blur-xl p-10 rounded-3xl border border-white/20 max-w-xl text-center"
        >
          <h1 className="text-4xl md:text-5xl font-bold mb-4">
            和風ポートフォリオ
          </h1>
          <p className="text-white/80 mb-6">
            Website pribadi dengan style Jepang dan background hidup.
          </p>
          <div className="flex justify-center gap-4">
            <button className="px-6 py-2 rounded-2xl bg-white text-black">
              Masuk
            </button>
            <button className="px-6 py-2 rounded-2xl border border-white">
              Tentang
            </button>
          </div>
        </motion.div>
      </main>
    </div>
  );
}